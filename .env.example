# --- PostgreSQL (docker-compose "db" service) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=asm@postgres
POSTGRES_DB=asm_db
POSTGRES_PORT=5432

# --- API (docker-compose "api" service) ---
APP_PORT=8000
